<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.VSGA_submission" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimaryDark">@color/blue</item>
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="Theme.VSGA_submission" parent="Base.Theme.VSGA_submission" />
</resources>